[package]
name = "asciirogue-combat"
description = "Combat system: attack / hit / damage / status / AI tick"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
asciirogue-core = { path = "../core" }
serde = { workspace = true }

